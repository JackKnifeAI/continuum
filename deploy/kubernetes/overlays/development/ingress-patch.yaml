apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: continuum-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  tls:
    - secretName: continuum-tls-dev
      hosts:
        - continuum-dev.example.com
  rules:
    - host: continuum-dev.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: continuum-api
                port:
                  name: http
